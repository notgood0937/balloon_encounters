services:
  balloon-encounters:
    build: .
    container_name: balloon-encounters
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - balloon-encounters-data:/app/data
    env_file:
      - .env.prod
    environment:
      - NODE_ENV=production

  caddy:
    image: caddy:2-alpine
    container_name: caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - balloon-encounters

volumes:
  balloon-encounters-data:
  caddy-data:
  caddy-config:
